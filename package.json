{
  "name": "23-headline-report",
  "version": "1.0.0",
  "description": "daily news report",
  "main": "server/server.js",
  "scripts": {
    "start": "npx nodemon server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "installer": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
